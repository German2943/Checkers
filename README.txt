how run:
1. download the .zip file.
2. go to Checkers-->Checkers .
3. inside there would be a folder named "CheckersGame", open it with the IDE that you use.


Currently the program has the following major issues or defects:
-the GUI is not completed.
-the "hitbox" of the pieces that you move in the board is a bit messed up, so it will
 show you the invalid-move-message unless you place each piece in a very specific way.
-still testing the registration of moves using the given format by the professor.